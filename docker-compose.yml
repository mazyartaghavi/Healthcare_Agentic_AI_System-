services:
  healthcare-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      HEALTHCARE_AGENT_DATABASE_URL: sqlite+pysqlite:////app/runtime/healthcare.db
      HEALTHCARE_AGENT_AUDIT_LOG_PATH: /app/runtime/audit.jsonl
    volumes:
      - ./runtime:/app/runtime
